--- a/src/github.com/AdguardTeam/AdGuardHome/internal/aghnet/net_linux.go
+++ b/src/github.com/AdguardTeam/AdGuardHome/internal/aghnet/net_linux.go
@@ -90,7 +90,7 @@ func ifaceHasStaticIP(ifaceName string)
 		filename string
 	}{{
 		FileWalker: iface.dhcpcdStaticConfig,
-		filename:   "/etc/dhcpcd.conf",
+		filename:   "/opt/etc/dhcpcd.conf",
 	}, {
 		FileWalker: iface.ifacesStaticConfig,
 		filename:   "/etc/network/interfaces",
@@ -141,13 +141,13 @@ func ifaceSetStaticIP(ifaceName string)
 	gatewayIP := GatewayIP(ifaceName)
 	add := dhcpcdConfIface(ifaceName, ipNet, gatewayIP, ipNet.IP)
 
-	body, err := os.ReadFile("/etc/dhcpcd.conf")
+	body, err := os.ReadFile("/opt/etc/dhcpcd.conf")
 	if err != nil && !errors.Is(err, os.ErrNotExist) {
 		return err
 	}
 
 	body = append(body, []byte(add)...)
-	err = maybe.WriteFile("/etc/dhcpcd.conf", body, 0o644)
+	err = maybe.WriteFile("/opt/etc/dhcpcd.conf", body, 0o644)
 	if err != nil {
 		return fmt.Errorf("writing conf: %w", err)
 	}
